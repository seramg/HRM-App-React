import styled from "styled-components";

const SkillsChipWrapper = styled.div`
justify-content:flex-start;
gap:10px;
    p{
        padding:5px;
        font-size:10px;
        color: white;
        background-color: #FF8AB6;
        border-radius:5px;
    }
  }
`;
export default SkillsChipWrapper;
